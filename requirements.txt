fastapi
uvicorn
langgraph
langchain
langchain-openai
langchain-text-splitters
langchain-chroma
chromadb

tavily-python
tenacity
pydantic
slowapi
python-dotenv
langsmith>=0.1.0
python-multipart

# PDF parsing for RAG uploads (layout & table-aware Markdown extraction)
pymupdf4llm>=0.0.17
pypdf

# Hybrid Search (BM25 + Vector RRF)
rank-bm25>=0.2.2

# Google Embedding API (free tier) — replaces local ONNX embeddings
google-genai>=1.0.0

# Postgres checkpointer
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.1.0

# Evaluation
watchdog>=4.0.0
watchdog>=4.0.0
clerk-backend-api>=1.0.0
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
langchain-community
langchain-text-splitters

# SSE Streaming
sse-starlette>=1.6.0

# Abuse Protection
python-magic; sys_platform != 'win32'
python-magic-bin; sys_platform == 'win32'
